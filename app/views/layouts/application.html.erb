<!DOCTYPE html>
<html>
  <head>
    <title>Rails Template</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
      * {
        
        padding-right: 5px;
        padding-left: 5px;
      }

      body,html {
        padding-bottom: 10px;
        padding-right: 0px;
        padding-left: 0px;
      }

      .prod-plan-row {
        display: flex;
        justify-content: space-around;
      }

      .space-qty-totals {
        display: flex;
        justify-content: space-around;
      }

      .cost-stats {
        max-width: 1000px;
        display: flex;
        justify-content: space-around;
      }

      .profit-stats {
        max-width: 800px;
        display: flex;
        justify-content: space-around;
      }
    </style>
  </head>

  <body>

      <nav class="navbar navbar-expand-lg bg-danger" data-bs-theme="dark" style="padding-left:0px; padding-right:0px;">
      <% if current_user != nil %>
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Ball Seed</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
              <a class="nav-link active" aria-current="page" href="/production_plans">All Plans</a>
              <a class="nav-link active" href="/users/edit">Edit Profile</a>
              <a class="nav-link active" href="/users/sign_out" data-method="delete">Sign Out</a>
            </div>
          </div>
        </div>
      <% else %>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
            <a class="nav-link active" href="/users/sign_up">Sign Up</a>
            <a class="nav-link active" href="/users/sign_in">Sign In</a>
          </div>
        </div>
      <% end %>
      </nav>

    <!--<hr>-->

    <%= yield %>
  </body>
</html>
